[build]
  base    = "frontend"   # optional but recommended
  publish = "."          # publish the contents of the base directory
  # command = ""         # no build needed for static files; leave blank or omit